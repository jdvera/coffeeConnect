// inpout a group #, or allow them to create one
    // top button: create group
        // user inputs group name
        // check if that name is being used
        // if not, create that group
        // if so, warn them and let them input a new one
    // bottom button: join group
        // check if name is being used
        // if yes, add them to group
        // if no, warn them and allow them to input a new one

// ask for their location (current or custom)

// Show results